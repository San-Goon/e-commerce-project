import HistoryPage from '@components/HitoryPage';
import HomeLayout from '@components/common/@Layout/HomeLayout';

const History = () => {
  return <HomeLayout content={<HistoryPage />} />;
};

export default History;
