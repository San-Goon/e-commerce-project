import PaymentPage from '@components/PaymentPage';
import HomeLayout from '@components/common/@Layout/HomeLayout';

const Payment = () => {
  return <HomeLayout content={<PaymentPage />} />;
};

export default Payment;
