import MyReviewPage from '@components/MyReviewPage';
import HomeLayout from '@components/common/@Layout/HomeLayout';

const MyReview = () => {
  return <HomeLayout content={<MyReviewPage />} />;
};

export default MyReview;
